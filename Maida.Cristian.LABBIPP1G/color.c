#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

int harcoderarColores(eColores color[], int tam, int cantidad)
{
    int error = -1
    if(mascota!= NULL && tam>0 && cantidad >=0 && cantidad <= tam)
    {
        error = 0;
        for(int i=0; i< cantidad; i++)
        {
            color[i].id = ids[i];
            strcpy(color[i].nombreColor, nombreColores[i]);
            error++;
        }
    }
    return error;
}
